import { Layout } from '~/layouts';

export default function Resume(): JSX.Element {
	return (
		<Layout.Default showBackround="false" seo={{ title: 'резюме ─ yune', }}>
			<div className="flex flex-grow min-h-full pt-16 pb-12">
			</div>
		</Layout.Default>
	);
}
